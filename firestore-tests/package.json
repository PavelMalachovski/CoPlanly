{
  "name": "coplanly-firestore-tests",
  "description": "Firestore security-rules tests for CoPlanly, run against the local emulator",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project demo-coplanly \"npm run test:only\"",
    "test:only": "mocha --timeout 20000 \"rules/**/*.test.js\"",
    "test:ci": "firebase emulators:exec --only firestore,storage --project demo-coplanly \"npm run test:only -- --reporter ../tools/mocha-ci-reporter.js\"",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "eslint": "^8.57.0",
    "firebase": "^11.0.0",
    "firebase-tools": "^15.28.1",
    "mocha": "^10.2.0"
  }
}
