# CoParently - Система совместного доступа к календарю

## 🎯 Обзор

Реализована полноценная система совместного доступа к календарю для совместного воспитания детей. Позволяет двум родителям:
- Обмениваться приглашениями
- Совместно управлять календарем событий
- Хранить и обновлять важную информацию о ребенке
- Получать уведомления о изменениях в реальном времени

## ✨ Основные функции

### ✅ 1. Система приглашений
- Приглашение второго родителя по email
- Принятие/отклонение приглашений
- Автоматическое создание связи между родителями

### ✅ 2. Данные о ребенке
- **Медикаменты**: название, дозировка, частота приема
- **Активности**: кружки, спорт, расписание
- **Аллергии**: список известных аллергенов
- **Школьная информация**: школа, учитель, класс
- **Экстренные контакты**: список контактов на случай ЧП

### ✅ 3. Совместный календарь
- Создание/редактирование/удаление событий
- Автоматическая синхронизация между пользователями
- Разграничение прав доступа (read/write)
- Отслеживание автора изменений

### ✅ 4. Push-уведомления
- Уведомления о новых событиях
- Уведомления об изменениях событий
- Уведомления о приглашениях
- Уведомления об обновлении данных о ребенке

### ✅ 5. Статус синхронизации
- Визуальные индикаторы синхронизации
- Прогресс бар при загрузке
- Отображение последней успешной синхронизации
- Уведомления об ошибках

## 🏗️ Архитектура

Проект следует **Clean Architecture** с тремя основными слоями:

```
┌─────────────────────────────────────┐
│     Presentation Layer              │
│  (UI, ViewModels, Compose)          │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Domain Layer                    │
│  (Models, Repository Interfaces)    │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Data Layer                      │
│  (Room, Firestore, FCM)             │
└─────────────────────────────────────┘
```

### Ключевые компоненты

#### Модели данных
- `Event` - Событие календаря (с поддержкой совместного доступа)
- `ChildInfo` - Информация о ребенке
- `User` - Профиль пользователя
- `Medication`, `Activity`, `EmergencyContact` - Вложенные модели

#### Сервисы
- `SyncService` - Управление синхронизацией
- `FcmService` - Управление push-уведомлениями
- `CoParentPairingService` - Управление приглашениями

#### UI Screens
- `CalendarScreen` - Главный экран календаря
- `ChildInfoScreen` - Просмотр данных о ребенке
- `PairingScreen` - Управление приглашениями
- `SettingsScreen` - Настройки приложения

#### Компоненты синхронизации
- `SyncStatusIndicator` - Полный индикатор статуса
- `SyncStatusIcon` - Компактная иконка
- `SyncStatusBadge` - Встраиваемый бадж

## 🚀 Быстрый старт

### Предварительные требования
- Android Studio Arctic Fox+
- JDK 17
- Firebase проект
- Min SDK 26 (Android 8.0)

### Установка

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/your-repo/coparently.git
cd coparently
```

2. **Настройте Firebase**
   - Создайте проект в [Firebase Console](https://console.firebase.google.com/)
   - Скачайте `google-services.json`
   - Поместите в `app/` директорию

3. **Разверните Firestore Rules**
```bash
firebase deploy --only firestore:rules
```

4. **Соберите проект**
```bash
./gradlew assembleDebug
```

### Базовое использование

#### Приглашение партнера
```kotlin
viewModel.sendInvitation("partner@example.com")
```

#### Создание события
```kotlin
val event = Event(
    id = UUID.randomUUID().toString(),
    title = "Футбол",
    startDateTime = LocalDateTime.now(),
    sharedWith = listOf(userId, partnerId),
    permissions = "read_write"
)
eventRepository.upsertEvent(event)
```

#### Добавление данных о ребенке
```kotlin
val childInfo = ChildInfo(
    id = UUID.randomUUID().toString(),
    childName = "Алиса",
    medications = listOf(
        Medication("Витамин D", "1000 IU", "Ежедневно")
    ),
    activities = listOf(
        Activity("Пианино", "Вторник 17:00", "Музыкальная школа")
    )
)
childInfoRepository.upsertChildInfo(childInfo)
```

## 📱 Скриншоты UI

### Календарь с событиями
- Отображение событий обоих родителей
- Цветовая кодировка (мама/папа)
- Индикаторы опеки

### Данные о ребенке
- Секции: медикаменты, активности, аллергии
- Экстренные контакты
- Школьная информация

### Статус синхронизации
- Индикатор в реальном времени
- Прогресс синхронизации
- Уведомления об ошибках

## 🔒 Безопасность

### Firestore Security Rules

```javascript
// События - только для владельца и партнера
match /events/{eventId} {
  allow read: if isAuthenticated() && (
    resource.data.createdByFirebaseUid == request.auth.uid ||
    request.auth.uid in resource.data.sharedWith
  );

  allow write: if isAuthenticated() &&
    request.auth.uid in resource.data.sharedWith;
}

// Информация о ребенке - общий доступ
match /child_info/{childInfoId} {
  allow read, write: if isAuthenticated() &&
    request.auth.uid in resource.data.sharedWith;
}
```

## 🔧 Конфигурация

### AndroidManifest.xml
```xml
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<meta-data
    android:name="com.google.firebase.messaging.default_notification_channel_id"
    android:value="coparently_notifications" />
```

### build.gradle.kts
```kotlin
dependencies {
    // Firebase
    implementation(platform("com.google.firebase:firebase-bom:32.7.0"))
    implementation("com.google.firebase:firebase-auth-ktx")
    implementation("com.google.firebase:firebase-firestore-ktx")
    implementation("com.google.firebase:firebase-messaging-ktx")

    // Hilt
    implementation("com.google.dagger:hilt-android:2.48")
    kapt("com.google.dagger:hilt-compiler:2.48")
}
```

## 📚 Документация

- [Детальная реализация](docs/shared-calendar-implementation.md)
- [Руководство по развертыванию](docs/deployment-guide.md)
- [Архитектура проекта](docs/project.md)

## 🧪 Тестирование

```bash
# Unit тесты
./gradlew test

# Instrumented тесты
./gradlew connectedAndroidTest
```

## 🤝 Contributing

1. Fork проект
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект находится под лицензией MIT - смотрите файл [LICENSE](LICENSE) для деталей.

## 👥 Авторы

- Ваше Имя - *Начальная работа*

## 🙏 Благодарности

- Firebase за backend инфраструктуру
- Jetpack Compose за современный UI toolkit
- Hilt за dependency injection
- Material 3 за дизайн систему

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
- Откройте [Issue](https://github.com/your-repo/coparently/issues)
- Посмотрите [Troubleshooting Guide](docs/deployment-guide.md#troubleshooting)
- Свяжитесь с разработчиками

---

**Примечание**: Это производственная версия с полной функциональностью. Для разработки используйте debug варианты и тестовые Firebase проекты.

