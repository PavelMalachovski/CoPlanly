package com.coparently.app.presentation.common

/** What an [InlineBanner] is telling the reader, which decides its colours. */
enum class BannerTone {
    /** A fact about the screen: shared, kept on this phone only, possibly out of date. */
    INFO,

    /** Something waits on the reader's answer. */
    ATTENTION
}
